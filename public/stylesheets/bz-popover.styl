.bz-popover,
.bz-tooltip
  position relative
  display inline-block

.bz-popover .popover,
.bz-tooltip .popover
  box-shadow(0 1px 5px 0 rgba(0, 0, 0, 0.13))
  display none
  font-family 'Helvetica',sans-serif
  margin-left 50%
  position absolute
  z-index 9999

.bz-tooltip .popover
  background-color color-gray-dark
  border-radius 4px
  color color-white
  font-size 14px
  opacity 0.9
  padding 8px
  text-align center
  width 112px

.bz-tooltip .popover
  max-height 200px
  overflow hidden

.bz-tooltip.top .popover
  bottom calc(100% + 8px)
  left -64px

.bz-tooltip.bottom .popover
  bottom -8px
  left -64px
  transform-translate(0, 100%)

.bz-tooltip.right .popover
  top 50%
  right -8px
  transform-translate(100%, -50%)

.bz-tooltip.left .popover
  top 50%
  left calc(-50% - 8px)
  transform-translate(-100%, -50%)

.bz-tooltip.top:hover .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-up .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-up .3s ease-in-out

.bz-tooltip.bottom:hover .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-down .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-down .3s ease-in-out

.bz-tooltip.left:hover .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-left .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-left .3s ease-in-out

.bz-tooltip.right:hover .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-right .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-right .3s ease-in-out

/*---- BLUES Popover ------------------------*/
.bz-popover .popover
  background-color color-white
  font-size 16px
  padding 0
  text-align left
  min-width 124px
  max-width 164px

.bz-popover .popover ul
  width 100%

.bz-popover .popover li
  cursor pointer
  padding 0 4px
  width 100%
  white-space nowrap

.bz-popover .popover li:hover
  background color-gray-light

.bz-popover .popover span
  color inherit
  display block
  height 100%
  padding 8px
  width 100%

.bz-popover .popover a
  color: inherit
  display block
  height 100%
  text-decoration none
  padding 8px
  width 100%

.bz-popover .popover
  max-height 300px
  overflow hidden
  overflow-y auto

.bz-popover.top .popover
  bottom calc(100% + 8px)
  left -64px

.bz-popover.bottom .popover
  bottom -8px
  left -64px
  transform-translate(0, 100%)

.bz-popover.right .popover
  top 50%
  right -8px
  transform-translate(100%, -50%)

.bz-popover.left .popover
  top 50%
  left calc(-50% - 8px)
  transform-translate(-100%, -50%)

.bz-popover.top.bz-on .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-up .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-up .3s ease-in-out

.bz-popover.bottom.bz-on .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-down .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-down .3s ease-in-out

.bz-popover.left.bz-on .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-left .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-left .3s ease-in-out

.bz-popover.right.bz-on .popover
  display block
  -webkit-animation fade-in .3s ease-in-out, shift-right .3s ease-in-out
  -moz-animation fade-in .3s ease-in-out, shift-right .3s ease-in-out

//@keyframes shift-up
//  from
//    bottom:30px
//  to
//    bottom: calc(100% + 8px)
//@keyframes shift-down {from{bottom:30px;} to{bottom:-8px;}}
//@keyframes shift-left {from{left:30px;} to{left:calc(-50% - 8px);}}
//@keyframes shift-right {from{right:30px;} to{right:-8px;}}